---
name: Lint YAML (reusable)

"on":
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  call-common-yamllint:
    uses: {{github_user}}/.github/.github/workflows/lint-yaml.yml@main
    secrets: inherit
