[project]
name = "uv"
version = "0.1.0"
description = "uv のドキュメントに書いてあることを検証するためのパッケージ"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["ruff>=0.14.1"]

[project.optional-dependencies]
dev = []

[dependency-groups]
dev = ["pytest>=9.0.3"]
