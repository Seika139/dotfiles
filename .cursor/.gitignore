# Cursor extensions directory (excluded from Git management)
extensions/

# Environment-specific files (excluded from Git management)
argv.json

# Other files that should not be managed
*.log
*.tmp
*.cache

# IDE state files (excluded from Git management)
ide_state.json
